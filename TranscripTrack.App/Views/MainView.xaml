<UserControl x:Class="TranscripTrack.App.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:CallMethodAction TargetObject="{Binding}" MethodName="OnLoaded" />
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="150" />
            <RowDefinition Height="50" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>

        <DatePicker Name="lineEntryDatePicker" SelectedDate="{Binding LineEntryDate}" Grid.Row="0" HorizontalAlignment="Center">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="SelectedDateChanged">
                    <b:CallMethodAction TargetObject="{Binding}" MethodName="OnEntryDateChanged" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
        </DatePicker>
        <ListView Name="profileSelectTable" SelectionMode="Single" Grid.Row="1" ItemsSource="{Binding LineRateEntries}" Width="320">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Line Type" Width="100" DisplayMemberBinding="{Binding LineRateType}" />
                    <GridViewColumn Header="# Lines" Width="100" DisplayMemberBinding="{Binding NumLines}" />
                    <GridViewColumn Header="Entered On" Width="120" DisplayMemberBinding="{Binding EnteredDate}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Grid.Row="2" Grid.Column="0" Command="{Binding AddLineRateEntryCommand}" Content="Add" Height="30" Width="60" />
        <Button Grid.Row="3" Grid.Column="1" Command="{Binding AddProfileCommand}"
                Name="addProfileButton" Content="Add A Profile" Height="30" Width="60" />

        <Button Grid.Row="3" Grid.Column="0"
                Command="{Binding SelectProfileCommand}" Content="Select A Profile" Height="30" Width="60" Margin="349,115,391,5" />
    </Grid>
</UserControl>
